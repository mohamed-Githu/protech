const Logo = ({ className }) => (
  <div className={className}>
    <img
      src="/images/logo.svg"
      className="cursor-pointer"
      width={140}
      height={50}
    />
  </div>
);

export default Logo;
